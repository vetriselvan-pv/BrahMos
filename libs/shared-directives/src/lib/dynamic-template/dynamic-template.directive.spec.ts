import { DynamicTemplateDirective } from './dynamic-template.directive';

describe('DynamicTemplateDirective', () => {
    it('should create an instance', () => {
        const directive = new DynamicTemplateDirective();
        expect(directive).toBeTruthy();
    });
});
