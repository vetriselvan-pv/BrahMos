import { Routes } from '@angular/router';
import { DBStudioContainerComponent } from './components/studio-container/studio-container.component';

export const routes: Routes = [
    {
        path: '',
        component: DBStudioContainerComponent,
    },
];
